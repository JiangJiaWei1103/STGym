# Configuration file for model architecture `STSGCN`
model_params:
    # Spatial/Temporal pattern extractor
    st_params:
        n_layers: 4
        h_dim: 64
        gcn_depth: 3
        n_series: 325
        act: "glu"
        temporal_emb_dim: 64
        spatial_emb_dim: 64
    in_dim: 1
    in_len: 12
    device: "cuda:0"
    out_len: 12

fit_params: