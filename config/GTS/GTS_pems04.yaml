# Configuration file for model architecture `GTS`
model_params:
    # Spatial/Temporal pattern extractor
    st_params:
        n_layers: 1
        enc_in_dim: 2
        dec_in_dim: 1
        h_dim: 64
        out_dim: 1
        n_adjs: 1
        max_diffusion_step: 3
        use_curriculum_learning: True
        cl_decay_steps: 2000
    # Graph structure learner
    gsl_params:
        train_ratio: 0.6
        dataset_name: pems04
        temperature: 0.5
    n_series: 307
    device: "cuda:0"
    out_len: 12

fit_params: